import React, { useState } from "react";
import withTracker from "../../simulate";

import ProjectInvitesList from "../../components/projectInvitesList";

export default withTracker(() => {
  return {};
})(({}) => {
  return <ProjectInvitesList />;
});
