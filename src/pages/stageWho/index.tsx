import React, { useState } from "react";
import withTracker from "../../simulate";

import Stage from "../../components/stageWho";

export default withTracker(() => {
  return {};
})(({}) => {
  return <Stage />;
});
